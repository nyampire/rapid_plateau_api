[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    -v
markers =
    integration: tests that require a real PostgreSQL/PostGIS database (run with --run-integration)
